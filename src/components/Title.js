import React from 'react'

const Title = ({ title }) => {
    return (
        <div className="main-title">
            <h1 className="title">{ title }</h1>  
        </div>
    )
}

export default Title;
